CREATE TABLE cursos (
                        id BIGINT NOT NULL AUTO_INCREMENT,
                        nome VARCHAR(150) NOT NULL,
                        categoria VARCHAR(100) NOT NULL,
                        PRIMARY KEY (id)
);